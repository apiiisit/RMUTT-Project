import { ScanStatusPipe } from './scan-status.pipe';

describe('ScanStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ScanStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
